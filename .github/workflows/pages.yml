name: Deploy GitHub Pages

# Disabled — site is hosted on VPS. Kept for optional fallback only.

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment is disabled. Use scripts/deploy-vps.sh"
